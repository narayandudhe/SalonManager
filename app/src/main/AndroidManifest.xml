<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salonmanageer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".customer_deatil_activity" />
        <activity android:name=".employee_detail_activity" />
        <activity android:name=".view_customer_activity" />
        <activity android:name=".view_employee_activity" />
        <activity android:name=".add_booking_activity" />
        <activity android:name=".add_service_activity" />
        <activity android:name=".add_sub_service_activity" />
        <activity android:name=".booking_detail_activity" />
        <activity android:name=".content_gallery_activity" />
        <activity android:name=".splash_activity"></activity>
    </application>

</manifest>